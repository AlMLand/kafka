 What set for: advertised.listeners

    - if clients are on private network:
        ~ the internal private IP
        ~ the internal private DNS hostname
      clients should be able to resolve the internal IP or hostname.

     - if clients are on public network:
        ~ the external public IP
        ~ the external public hostname pointing to the public IP
      clients must be able to resolve the public hostname.